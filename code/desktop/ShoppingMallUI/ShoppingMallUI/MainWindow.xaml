<Window x:Class="ShoppingMall.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Mall" WindowState="Maximized" Background="#FF251D23" FontFamily="Tempus Sans ITC" FontSize="20">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ControlStyle;component/Commonstyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <DockPanel LastChildFill="True">
        

        <UniformGrid DockPanel.Dock="Top" Columns="1" Background="#FF251D23">
            <Label Name="lable1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" FontFamily="Snap ITC" FontSize="48" Foreground="Goldenrod"
              Content="Welcome to the Shoppingmall">
                <Label.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation AutoReverse="True" From="2" To="0" Storyboard.TargetName="lable1" Storyboard.TargetProperty="Opacity" RepeatBehavior="Forever">

                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Label.Triggers>
            </Label>
        </UniformGrid>
        <UniformGrid  DockPanel.Dock="Top" Rows="1" >
            <DockPanel>
                <Button DockPanel.Dock="Right" Style="{StaticResource ControlBtnStyle}">Go</Button>

                <TextBox DockPanel.Dock="Right"  Width="276" HorizontalAlignment="Left" />
                <!--<Image Source=""></Image>-->
                
                <Label DockPanel.Dock="Right"></Label>
            </DockPanel>

                    


        </UniformGrid>
        <UniformGrid DockPanel.Dock="Top" Margin="2">
            <TextBlock FontFamily="Sylfaen" FontWeight="Bold" FontStyle="Normal" >
                <Button Name="product" Style="{StaticResource ControlBtnStyle}" Click="product_Click">Products</Button>
                <Button Name="shop" Style="{StaticResource ControlBtnStyle}" Click="shop_Click">Shops</Button>
                <Button Name="floor" Style="{StaticResource ControlBtnStyle}" >Floor</Button>
                <Button Name="contactusBtn" Style="{StaticResource ControlBtnStyle}" Click="contactusBtn_Click" >Contact Us</Button>
                <Button Name="adminZoneobjHL" Style="{StaticResource ControlBtnStyle}" Click="adminZoneobjHL_Click" >Admin Zone</Button>
            </TextBlock>
        </UniformGrid>
        <DockPanel DockPanel.Dock="Bottom" Name="infodocP" LastChildFill="True">
            <UniformGrid DockPanel.Dock="Top">
                <UniformGrid.Background>
                    <ImageBrush ImageSource="/ShoppingMallUI;component/Images/shopping-femmes-hd-fond-d-cran-182156.jpg" />
                </UniformGrid.Background>
            </UniformGrid>
        </DockPanel>

    </DockPanel>
</Window>
